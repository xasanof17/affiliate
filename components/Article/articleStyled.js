import styled from "styled-components";

export const BlogViews = styled.div``;

export const BlogViewsLink = styled.div`
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 8px;
  cursor: pointer;
  span {
      user-select: none;
    font-weight: 400;
    font-size: 16px;
    line-height: 20px;
    color: var(--main-color);
  }
`;